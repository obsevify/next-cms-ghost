// This allows an alternative site title for meta data for pages.
export const siteTitleMeta = `Next.js Headless Ghost Casper`

// This allows an alternative site description for meta data for pages.
export const siteDescriptionMeta = `Next.js with Ghost CMS and Casper Skin`

// Used for App manifest e.g. Mobile Home Screen
export const shortTitle = `Headless Ghost`

// Logo + SiteImage in /public dir used for SEO, RSS, and App manifest
export const siteIcon = `favicon.png`
export const siteImage = `site-meta.png`
export const backgroundColor = `#e9e9e9`
export const themeColor = `#15171A`

// Customize navigation menu (default: []), label is case sensitive
// Example override: [{ label: `Home`, url: `/` }]
// Example add new: [{ label: `Contact`, url: `/contact` }]
export const customNavigation = [{ label: `Contact`, url: `/contact` }]

// Exclude post or pages (default: do not exclude)
//export const constexcludePostsOrPages = (() => false)

